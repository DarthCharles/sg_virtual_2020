import { AppRegistry } from 'react-native';
import { App } from 'components/src/App';

AppRegistry.registerComponent('sg_virtual_2020', () => App);
AppRegistry.runApplication('sg_virtual_2020', {
  rootTag: document.getElementById('root'),
});
